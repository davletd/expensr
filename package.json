{
  "name": "expensr",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "start": "turbo start",
    "build": "turbo build",
    "test": "turbo test",
    "dev": "turbo run start --parallel"
  },
  "devDependencies": {
    "turbo": "^1.2.0"
  }
}